monitor:
  interval_s: 3
  targets:
    - name: "web health"
      type: "http"
      url: "http://172.30.0.20:8080/health"
      timeout_s: 2
      warn_ms: 250
      crit_ms: 600
    - name: "controller health"
      type: "http"
      url: "http://172.30.0.10:8000/health"
      timeout_s: 2
      warn_ms: 250
      crit_ms: 600
  metrics_path: "/data/metrics.jsonl"
  alert:
    # ALERT_WEBHOOK_URL env가 있으면 웹훅으로 보내고 없으면 콘솔만 출력
    enabled: true
